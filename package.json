{
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  },
  "scripts": {
    "setup": "npm install && npx playwright install",
    "test": "npx playwright test"
  },
  "dependencies": {
    "dotenv": "^16.3.1"
  }
}
