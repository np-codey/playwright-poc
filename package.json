{
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  },
  "scripts": {
    "setup": "npm install && npx playwright install",
    "test": "npx playwright test --project=main"
  },
  "dependencies": {
    "@types/node": "^20.10.0",
    "dotenv": "^16.3.1"
  }
}
